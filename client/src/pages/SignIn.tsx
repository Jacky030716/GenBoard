import { SignInForm } from "@/features/auth/components/SignInForm";

const SignIn = () => {
  return (
    <main className="flex flex-col justify-center items-center bg-white w-full h-full">
      <SignInForm />
    </main>
  );
};

export default SignIn;
